# Multiturn

## How to test sandbox

Run `start_docker.sh` to expose the docker daemon over TCP, then do `docker compose up` to start the other containers, then run `sandbox.py`. The test script just starts some sandboxes to test them.