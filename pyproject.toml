[project]
name = "multiturn-rl"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = ["verl[gpu,vllm]"]

[tool.uv.sources]
verl = { path = "verl", editable = true }
